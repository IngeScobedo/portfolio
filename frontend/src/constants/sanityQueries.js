const aboutsQuery = '*[_type == "abouts"]';
const worksQuery = '*[_type == "works"]';
const experienciesQuery = '*[_type == "experiences"]';
const skillsQuery = '*[_type == "skills"]';

export default {
    aboutsQuery,
    worksQuery,
    experienciesQuery,
    skillsQuery,
}